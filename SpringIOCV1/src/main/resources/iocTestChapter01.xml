<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--
        一个Bean标签可以注册一个组件（对象、类）
        class：写要注册的组件的全类名
        id：这个对象的唯一标示
        bean默认是单例的，可以通过设置 scope="prototype" 来设置该bean为非单例（scope对应的singleton为单例，不写默认也是singleton）
    -->
    <bean id="product" class="ProjectC.Gold.Note.SpringIOCV1.pojo.Product" scope="prototype">

        <!--
            使用property标签为Product对象的属性赋值
            name="xxx"：指定属性名
            value="xx"：为这个属性赋值
            只要是基本数据类型都可以直接写值，spring会做转换。
            如果不是基本数据类型的情况（比如说类对象），需要在property标签中进一步配置。
            换句话说，只要是复杂类型都需要在property标签中进一步配置。
            复杂类型配置请参考 ProjectC.Gold.Note.SpringIOCV1.IOCTest.IOCTestChapter02
        -->
        <property name="id" value="1"/>
        <property name="name" value="Nokia970"/>
        <property name="price" value="2999"/>

    </bean>

    <bean id="category" class="ProjectC.Gold.Note.SpringIOCV1.pojo.Category">
        <property name="id" value="2"/>
        <property name="name" value="Apple Line"/>
    </bean>

    <bean id="cat" class="ProjectC.Gold.Note.SpringIOCV1.pojo.Cat" scope="prototype">
        <property name="kind" value="短腿"/>
        <property name="age" value="1"/>
        <!--
            由于没有给name属性设置setter方法所以会报错
        -->
        <property name="name" value="饭饭"/>
    </bean>

</beans>